const DB_NAME = 'codecamp';
const DB_USERNAME = 'root';
const DB_PASSWORD = 'sha_1996MYSQL';
const DB_HOST = 'localhost';
const DB_DIALECT = 'mysql';

const JWT_KEY = 'SECRET_KEY';

module.exports = {
  DB_NAME,
  DB_USERNAME,
  DB_PASSWORD,
  DB_HOST,
  DB_DIALECT,

  JWT_KEY
};
